import { ChangeOnScrollDirective } from './change-on-scroll.directive';

describe('ChangeOnScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeOnScrollDirective();
    expect(directive).toBeTruthy();
  });
});
